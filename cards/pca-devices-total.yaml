kind: card
key: pca-devices-total
name: Devices — total
description: Current snapshot, not historised — device.state has no history table, so this is a live count of every row in device, regardless of state.
collection: pear-cider-automated
type: question
display: scalar
cache_ttl: null
archived: false
dataset_query:
  lib/type: mbql/query
  database: 2
  stages:
  - lib/type: mbql.stage/native
    native: |
      -- Current snapshot only: device.state is not historised.
      SELECT count(*)::bigint AS devices_total
      FROM device
visualization_settings: {}
parameters: []
